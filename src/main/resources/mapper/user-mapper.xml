<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neu.reliidentyBack.dao.UserMapper">
    <sql id="selectFields">
        username,password,salt,usertype,create_time
    </sql>
    <sql id="insertFields">
        id,username,password,salt,usertype,create_time
    </sql>
    <resultMap id="userResultMap" type="com.neu.reliidentyBack.domain.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="Type" column="usertype" javaType="com.neu.reliidentyBack.reliidentyUtils.enums.UserTypeEnum" typeHandler="com.neu.reliidentyBack.reliidentyUtils.EnumTypeHandler"/>
        <result property="salt" column="salt"/>
        <result property="createTime" column="create_time"/>

    </resultMap>
    <select id="selectUserById" resultMap="userResultMap">
        select <include refid="selectFields"></include>
        from user
        where id=#{id}
    </select>
    <select id="selectUserByName" resultMap="userResultMap">
        select <include refid="selectFields"></include>
        from user
        where username=#{username}
    </select>
    <insert id="insertUser" parameterType="com.neu.reliidentyBack.domain.User" keyProperty="id" >
        insert into user(<include refid="insertFields"></include>)
        values(#{username},#{password},#{salt},#{Type},#{createTime})
    </insert>
</mapper>
